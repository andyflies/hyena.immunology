
# Sets the working directory to the R root directory.  
setwd("C:\\Users\\AF\\Documents\\My Dropbox")

# Remove all objects from the R root directory.  Returns warning messages if the objects have already been removed. This is OK.
rm(df.data)
rm(list=ls(all=TRUE))

